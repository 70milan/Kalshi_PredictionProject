# Phase 4.1 Implementation Plan: The Intelligence Ledger

This plan implements the "Storage" half of the Inference Engine. We will ensure that every intelligence brief generated by Gemini is saved back into the Gold layer as a queryable record.

## User Review Required

> [!NOTE]
> **Schema Design**: We will save the following fields: `ticker`, `current_odds`, `bull_case`, `bear_case`, `verdict`, and `ingested_at`. This allows for historical tracking of how AI verdicts change as news evolves.

## Proposed Changes

### 1. Inference Engine Storage

#### [MODIFY] [explain_mispricing.py](file:///c:/Data%20Engineering/codeprep/predection_project/inference/explain_mispricing.py)
Adding the persistent storage logic.
- **Collection**: Modify the `main()` loop to collect results into a list of dictionaries.
- **Storage Function**: Implement `save_briefs_to_gold(results)` using Pandas.
- **Path**: Save to `data/gold/intelligence_briefs`.

## Verification Plan

### Automated Tests
1. **End-to-End Save**: Run the Inference Engine (mocking a price move if necessary) and verify that a `.parquet` file is created in `data/gold/intelligence_briefs`.

### Manual Verification
1. **DBeaver Check**: Open DBeaver and run `SELECT * FROM delta_scan('data/gold/intelligence_briefs')` to see the AI's verdicts in table format.
